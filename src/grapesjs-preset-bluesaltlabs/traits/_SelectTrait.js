/** https://github.com/artf/grapesjs/blob/dev/src/trait_manager/view/TraitView.js **/

// Local Constants
import {
  TRAIT_SELECT,
} from '../constants'

// todo: not 100% sure if this is the right way to modify an existing trait.
// this is the default trait.
export function SelectTrait(traitManager, config, editor) {
//  const textType = traitManager.getType(TRAIT_SELECT)
//
//  traitManager.addType(TRAIT_SELECT, {
//    ...textType.extend({
//      // todo?
//    })
//  })
}
