/** https://github.com/artf/grapesjs/blob/dev/src/trait_manager/view/TraitNumberView.js **/

// Local Constants
import {
  TRAIT_NUMBER,
} from '../constants'

// todo: not 100% sure if this is the right way to modify an existing trait.
export function NumberTrait(traitManager, config, editor) {
//  const numberType = traitManager.getType(TRAIT_NUMBER)
//
//  traitManager.addType(TRAIT_NUMBER, {
//    ...numberType.extend({
//      // todo?
//    })
//  })
}
