/** https://github.com/artf/grapesjs/blob/dev/src/trait_manager/view/TraitColorView.js **/

// Local Constants
import {
  TRAIT_COLOR,
} from '../constants'

// todo: not 100% sure if this is the right way to modify an existing trait.
export function ColorTrait(traitManager, config, editor) {
//  const colorType = traitManager.getType(TRAIT_COLOR)
//
//  traitManager.addType(TRAIT_COLOR, {
//    ...colorType.extend({
//      // todo?
//    })
//  })
}
