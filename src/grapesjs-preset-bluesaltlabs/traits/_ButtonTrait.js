/** https://github.com/artf/grapesjs/blob/dev/src/trait_manager/view/TraitButtonView.js **/

// Local Constants
import {
  TRAIT_BUTTON,
} from '../constants'

// todo: not 100% sure if this is the right way to modify an existing trait.
export function ButtonTrait(traitManager, config, editor) {
//  const buttonType = traitManager.getType(TRAIT_BUTTON)
//
//  traitManager.addType(TRAIT_BUTTON, {
//    ...buttonType.extend({
//      // todo?
//    })
//  })
}
